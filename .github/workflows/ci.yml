name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Sync shortcuts
        if: hashFiles('Scripts/sync-shortcuts.sh') != ''
        run: ./Scripts/sync-shortcuts.sh

      - name: Build
        run: swift build

      - name: Test
        run: swift test
